declare module "@mailchimp/mailchimp_marketing" {
  // If you just want to silence the error, you can export `any` types:
  const content: any;
  export default content;
  export const setConfig: any;
  export const lists: any;
  // etc... (Or define more specific types if desired)
}
